<h1>
Home Page
</h1>

<p>
Current User List Below
</p>

  <ul>
    <% @users.each do |user| %>
    <li>
      <%= link_to "ID: #{user.id}", "/users/#{user.id}" %>
      <%= link_to "Edit Profile", "/users/#{user.id}/edit" %>
      Alias: <%= user.alias %>
      Email: <%= user.email %>
    </li>
    <% end %>
  </ul>
